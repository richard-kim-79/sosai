services:
  - type: web
    name: sosai-backend
    env: python
    runtime: python3
    pythonVersion: 3.9.12
    rootDir: ai-service
    buildCommand: |
      python -m pip install --upgrade pip
      pip install --no-cache-dir -r requirements.txt
    startCommand: gunicorn app:app --workers 4 --worker-class uvicorn.workers.UvicornWorker --bind 0.0.0.0:$PORT --timeout 120
    envVars:
      - key: OPENAI_API_KEY
        value: your_openai_api_key_here
      - key: PORT
        value: 8001
    healthCheckPath: /
    autoDeploy: true

  - type: web
    name: sosai-frontend
    env: node
    buildCommand: cd ai-service/frontend && npm install && npm run build
    startCommand: cd ai-service/frontend && npm run preview
    envVars:
      - key: VITE_API_URL
        value: https://sosai-backend.onrender.com
      - key: PORT
        value: 10000 